# devpace 项目目标

> **职责**：定义项目的愿景、业务目标和成效指标。这是项目决策的最终依据。

## 北极星

**devpace 给 Claude Code 项目一个稳定的研发节奏——需求在变，节奏不乱。**

- 会话断了，节奏自动续上
- 需求变了，影响自动评估，有序调整
- 质量跑偏，检查自动纠正

> **需求在变是常态，失序不应该是。**

## 目标用户

**使用 Claude Code 持续迭代产品的开发者**——不是写一次性脚本，而是在多个会话中推进一个有业务目标的项目。

## 三个价值视角

### 客户价值

解决开发者在使用 Claude Code 做产品研发时的四个核心痛点：

| 痛点 | 表现 | devpace 的价值主张 |
|------|------|---------------------|
| 跨会话连续性断裂 | 每次开 Claude 都要重新解释"在哪、干嘛" | 自动恢复上下文，零手动解释 |
| 质量不一致 | Claude 有时跳过测试、忘记检查 | 质量检查体系自动执行，不可跳过 |
| 价值与技术脱节 | 做着做着偏离业务目标 | 始终可追溯——从业务目标到功能到代码变更，追溯能力随迭代自然加深 |
| 需求变更导致失序 | 需求变了不知道影响多大，进度混乱 | 变更影响分析 + 有序调整 |

### 企业价值

- Claude 的工作过程可审计、可追溯、质量可控
- 多项目并行时不丢上下文
- 研发效能可度量、可改进

### 生态价值

- 填补 Claude Code 生态中"研发流程管理"的空白
- 不替代 GitHub Issues/Jira，而是补充——管的是"Claude Code 会话级的研发节奏"
- 开源社区可贡献、可适配

## 差异化定位

与纯任务管理工具（TodoWrite、GitHub Issues）的区别：

| 维度 | 任务管理工具 | devpace |
|------|------------|-----------|
| 核心模型 | 任务列表 | 价值交付链路 |
| 对变更的态度 | 计划是稳定的 | **变更是一等公民** |
| 与 Claude 的关系 | Claude 是执行者 | Claude 是自治协作者 |
| 可追溯性 | 任务 → 代码 | 业务目标 → 功能 → 变更 → 代码（始终可追溯，随迭代加深） |
| 度量 | 完成数 | 质量 + 价值对齐 |

### 护城河层次

| 能力层 | devpace 价值 | 被原生替代风险 |
|--------|-------------|---------------|
| 跨会话连续性 + 轻量追溯 | 用户入口 | 高——可能被原生 session persistence 替代 |
| 功能级变更管理 | 差异化所在 | 低——需要概念模型驱动的流程引擎 |
| 完整价值链 + 度量 | 不可替代的护城河 | 极低——需要完整 BR→PF→CR 链路 |

战略含义：跨会话连续性是获取用户的入口，变更管理是差异化，完整价值链 + 度量是长期不可替代的价值。即使 Claude Code 原生增强了会话持久化，devpace 在变更管理和价值链层的价值不受影响。

## 业务目标

### MVP 阶段（1-2 周）

| ID | 目标 | 成效指标（MoS） | 验证方式 |
|----|------|---------------|---------|
| OBJ-1 | 跑通完整工作流 | 1 个 CR 从 created→merged 全流程走通 | 在外部项目（非 devpace 自身）实际执行 |
| OBJ-2 | 跨会话连续性 | 连续 3 次会话，Claude 无需用户重述之前的工作即可继续 | 模拟中断恢复场景 |
| OBJ-3 | 质量检查有效 | 质量检查发现至少 1 个本会遗漏的问题 | 回顾质量检查记录，确认有价值发现 |
| OBJ-4 | 变更管理有价值 | 需求变更后 30 秒内恢复有序状态 | 模拟 3 种变更场景，计时恢复速度 |
| OBJ-5 | devpace 优于手动方案 | 与 CLAUDE.md 手动方案对比：上下文恢复准确率更高 | 同一项目分别用两种方案，对比恢复质量 |
| OBJ-6 | 渐进丰富自然 | 概念模型信息渐进丰富时用户无感知负担 | 用户事后确认未感受到复杂度增加 |

### 成熟阶段（1 个月）

| ID | 目标 | 成效指标（MoS） |
|----|------|---------------|
| OBJ-7 | 可移植性验证 | 新项目从 /pace-init 到首个 CR merged 不超过 1 小时 |
| OBJ-8 | 度量体系可用 | /pace-retro 生成有意义的回顾报告 |

### 生态阶段（长期）

| ID | 目标 | 成效指标（MoS） |
|----|------|---------------|
| OBJ-9 | 社区可用 | 新用户 5 分钟内跑通 /pace-init |
| OBJ-10 | 需求变更管理成为差异化卖点 | 用户反馈中"变更处理"出现频率 > 30% |
