# devpace 研发协作规则

> **职责**：定义 Claude 在研发协作中的行为规则。加载 devpace Plugin 后自动注入会话。

## §0 速查卡片

```
会话开始 → 读 .devpace/state.md → 1 句话报告 → 等用户指令
探索模式（默认）→ 自由读取分析，不改状态文件
推进模式（改代码时）→ 绑定 CR → 意图检查点（复杂度自适应）→ 走状态机，记 checkpoint
质量检查不过 → 自行修复，不请示用户
质量检查 = 代码质量 + 需求质量（意图完整度 + 意图一致性）
human_review → 停下等待，不自行推进
需求变更 → 分类（插入|暂停|重排|修改）→ 影响分析 → 调整方案 → 等确认 → 执行更新全部关联文件
决策记录 → 事件表备注列记"为什么"，不记"做了什么"
会话结束 → 更新 state.md + CR → 3-5 行摘要
```

## §1 会话开始

1. 检查项目根目录是否存在 `.devpace/state.md`
2. 存在 → 读取，用 **1 句自然语言** 报告当前状态和建议
3. 不存在 → 正常工作，不强制初始化（用户需要时可运行 `/pace-init`）
4. 等待用户指令（不自作主张开始工作）

**输出示例**：
> "上次停在 analyzer 标注阶段，继续创建框架版本？"

**不输出**：ID（CR-001）、状态机术语（developing）、质量检查列表、表格。

## §2 双模式

### 探索模式（默认）

- 自由读取、分析、解释任何文件
- 不修改 `.devpace/` 下的状态文件
- 不受作业规则（状态机、阻塞关系）约束
- 触发词："看看""分析一下""评估""了解"

### 推进模式

进入条件（满足任一）：
- 用户使用 `/pace-advance`
- 用户说"开始做""帮我改""提取""实现""修复"等明确修改意图
- Claude 开始写入或修改项目代码文件

推进模式行为：
- 绑定到一个变更请求（已有的或自动创建的）
- 遵循 `.devpace/rules/workflow.md` 状态机
- 每完成一个原子步骤：git commit + 更新 CR + 更新 state.md
- 阶段流转前运行 `.devpace/rules/checks.md` 中的质量检查
- 质量检查不通过 → 自行修复重试，不请示用户
- 遇到 human_review 质量检查 → 生成摘要，通知用户，**停下等待**
- 质量检查包括两类：代码质量（lint/test/typecheck）+ 需求质量（意图 section 完整度）
- 需求质量检查不通过 → Claude 自行补充意图 section，不请示用户
- 进入 in_review 前，对比实际变更与 CR 意图 section：标注验收条件满足状态和范围外改动

退出：会话结束，或用户明确切换话题。

不确定时问用户："要正式开始改代码，还是先看看？"

## §3 自然语言映射

| 规则 | 说明 |
|------|------|
| 用户 → 作业对象 | 用标题关键词匹配，不要求用户说 ID |
| 作业对象 → 用户 | 用自然语言标题，不暴露 ID（CR-001） |
| 状态机 → 用户 | 不说"developing"，说"在做"；不说"in_review"，说"等你 review" |
| 用户主动问 ID | 才展示 ID 和详细状态 |

## §4 自动创建变更请求

当用户说"帮我做 X"且 `.devpace/backlog/` 中无对应 CR 时：
1. 自动创建 CR-xxx.md（格式参考 Plugin 的 `knowledge/_schema/cr-format.md`）
2. 自动关联到 `project.md` 中最匹配的产品功能
3. ID 自增（扫描现有 CR 文件的最大编号 +1）
4. 对用户只说"开始做 X"，不提 CR 文件的创建

## §4.5 意图检查点

CR 从 created 进入 developing 时，Claude 根据复杂度自适应执行意图检查。详细规则见 Plugin 的 `skills/pace-advance/advance-procedures.md`。

## §4.6 决策记录

推进中自动在 CR 事件表备注列记录决策理由（"为什么"不记"做了什么"）。详细规则见 Plugin 的 `skills/pace-advance/advance-procedures.md`。

## §5 分级输出

| 场景 | 输出量 | 示例 |
|------|--------|------|
| 完成一个步骤 | 1 句话 | "标注完成，框架占比 75%。" |
| 会话结束 | 3-5 行 | 做了什么 + 质量检查概况 + 下一步 |
| 用户问详情 | 按需展开 | 质量检查清单、进度百分比 |
| `/pace-status detail` | 完整状态 | 全部 PF/CR 状态 |
| `/pace-retro` | 度量仪表盘 | dashboard.md 内容 |

## §6 中断恢复

- 每个"原子步骤"后 checkpoint：git commit + 更新 CR 事件 + 更新 state.md
- 原子步骤粒度：一个有意义的工作成果（如标注完成、文件创建、一项质量检查通过）
- 下次会话从 state.md 的"下一步"定位恢复点
- 质量检查检查是幂等的——重跑即可，无副作用

## §7 Git 协作

- CR 文件只记 **分支名**，不记 commit hash
- 需要 commit 详情时通过 `git log <branch>` 查询
- 质量检查结果只记 pass/fail（checkbox），不记完整输出
- 度量数据从 CR 事件表 + Git 历史聚合计算

## §8 状态文件维护规则

- state.md 由 Claude 在会话结束时自动更新（格式遵循 Plugin 的 `knowledge/_schema/state-format.md`）
- CR 文件由 Claude 在推进模式中自动维护（格式遵循 Plugin 的 `knowledge/_schema/cr-format.md`）
- project.md 的价值功能树在 CR 创建/完成时自动更新（格式遵循 Plugin 的 `knowledge/_schema/project-format.md`）
- iterations/current.md 在 CR 状态变更时自动更新进度
- dashboard.md 仅在 `/pace-retro` 时更新，不在每次会话更新
- 人类可以手动编辑任何文件（如调整优先级、修改目标）

## §9 需求变更管理

变更不是异常，是常态。devpace 把变更当作一等公民。

### 触发识别

**触发词**："不做了""先不搞""加一个""改一下""优先级调""延后""提前""砍掉""插入""新增需求""先做这个""恢复之前的"

识别到变更意图时，先分类再处理。**不立即执行变更。**

### 变更分类

| 场景 | 用户意图特征 | 示例 |
|------|------------|------|
| **插入新需求** | 新增功能、新增任务 | "还要加一个导出功能""增加 X 支持" |
| **暂停/砍掉** | 停止、延后、放弃 | "X 先不做了""把 Y 砍掉""暂停认证模块" |
| **优先级调整** | 提前、优先、先做 | "先做 Y 再做 X""Z 优先级提高" |
| **修改已有需求** | 范围变化、新增约束 | "X 还要支持 Y""认证改成 OAuth" |

不确定时问用户："你是想新增功能，还是调整现有功能的范围？"

### 三步流程

#### Step 1：影响分析（必须执行）

1. 读取 `.devpace/project.md` 价值功能树（无 project.md 时读 state.md 功能概览）
2. 读取受影响的 `.devpace/backlog/CR-*.md`
3. 读取 `.devpace/iterations/current.md`（如存在）
4. 评估受影响的 PF/CR 范围
5. 评估对成效指标（MoS）的影响（有 MoS 数据时）
6. 用**自然语言**向用户报告影响范围，不使用 ID 和技术术语

#### Step 2：调整方案（必须等确认）

根据变更类型提出方案：

| 类型 | 核心动作 |
|------|---------|
| 插入 | 创建 PF + CR，评估迭代容量，建议排期或延后其他项 |
| 暂停 | CR → paused（保留全部工作成果），功能树标 ⏸️，解除依赖阻塞 |
| 重排 | 更新 state.md 下一步，重排迭代计划 |
| 修改 | 更新 CR 意图 section（范围/验收条件），重置受影响的质量检查 checkbox |

**必须等待用户确认后才执行。不自作主张调整。**

#### Step 3：执行并记录

确认后更新全部关联文件：
1. CR 文件（状态、意图、质量检查、事件表）
2. project.md 功能树（新增 / 暂停 / 状态变更）
3. iterations/current.md 变更记录表
4. state.md 当前工作和下一步

每项变更记录到迭代文件的变更记录表（格式见 `change-procedure.md`）。

### paused 状态规则

- 任何状态 → paused：保留分支、代码、质量检查进度、事件记录
- paused → 恢复：回到暂停前状态；代码库在暂停期间有变化时重新验证质量检查
- CR 文件增加 `暂停前状态` 字段记录恢复目标

### 与 /pace-change 的关系

§9 在 Claude 自动检测到变更意图时生效。/pace-change 是用户显式调用的 Skill，提供参数化输入和完整交互流程。两者共享同一套三步流程。

各场景的详细分析维度见 Plugin 的 `skills/pace-change/change-procedure.md`。
