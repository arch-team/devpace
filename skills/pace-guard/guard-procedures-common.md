# pace-guard 通用规程

> **职责**：跨子命令共享规则——严重度矩阵（SSOT）、分级自主矩阵（SSOT）、分层输出约定（SSOT）、风险文件创建规则、降级模式。按 SKILL.md 路由表按需加载。

## §0 速查卡片

| 子命令 | 文件 | 核心内容 |
|--------|------|---------|
| scan | scan | 5 维扫描 + 复杂度自适应 + 异常驱动输出 + 意图检查点集成 |
| monitor | monitor | 运行时风险摘要 + 三层分层输出 |
| trends | trends（自包含） | 跨迭代风险分布 + 老化检测 + 重复 Pattern |
| report | report（自包含） | 所有 open 风险 + 严重度分布 + 优先级建议 |
| resolve | resolve | 状态转换 + 批量 resolve + 自动提议 |

- **SSOT 内容**：本文件是严重度矩阵、分级自主矩阵、分层输出约定、智能推荐模板、风险文件创建规则的唯一权威源
- **A-编号索引**：A1 复杂度自适应(scan) · A2 分层输出(monitor) · A3 异常驱动报告(scan) · A4 批量处理(resolve) · A6 分层输出(trends) · A7 中断恢复(scan) · B3 风险老化(trends)

## 严重度判定矩阵（SSOT）

| 维度 | Low | Medium | High |
|------|-----|--------|------|
| 历史教训 | 无匹配 defense pattern | 有匹配 defense pattern | 同一模块连续 2+ 次打回 |
| 依赖影响 | ≤3 个文件引用 | 4-10 个文件引用 | >10 个文件引用 |
| 架构兼容性 | 无 context.md 或无约定冲突 | 边界模糊的约定 | 明确违反已声明约定 |
| 范围复杂度 | 实际 = 预期 | 实际 > 预期 1 级（如 S→M） | 实际 > 预期 2+ 级（如 S→L） |
| 安全敏感度 | 不涉及安全文件 | 涉及安全相关模块 | 涉及加密/权限核心代码 |

综合风险等级 = 取所有维度的最高等级。

## 分级自主矩阵（SSOT）

读取 `.devpace/project.md` 的自主级别字段（默认"标准"）。

| 项目自主级别 | Low 风险 | Medium 风险 | High 风险 |
|------------|---------|------------|----------|
| 辅助 | 记录+简要提醒 | 记录+详细提醒+方案 | 暂停+等用户确认 |
| 标准 | 静默记录 | 记录+提醒+方案 | 暂停+等用户确认 |
| 自主 | 静默记录 | 记录+自动缓解+报告 | 提醒+方案+等用户确认 |

**High 风险不可绕过人类确认**——这是铁律，与 Gate 3 审批不可绕过同级别。

## 分层输出约定

> 三级输出详细度定义见 `knowledge/_guides/output-guide.md §分层输出约定`（SSOT）。

**pace-guard 自动升级规则**：各子命令根据上下文自动提升输出层级（具体规则见各子命令规程文件的"自动升级规则"表）。

## 新项目风险扫描兜底

当 `.devpace/insights.md` 不存在或内容为空（无 pattern 条目）时，对前 3 个 CR 自动触发基础风险扫描：

**检测条件**（全部满足）：
1. `.devpace/insights.md` 不存在或无 `## defense` / `## success` / `## improvement` 条目
2. `.devpace/backlog/` 中 CR 文件总数 ≤ 3（含当前创建的）

**触发行为**：
- 满足条件时，无论 CR 复杂度（S/M/L/XL），均执行基础 3 维扫描（历史教训跳过，仅扫描：依赖影响、架构兼容性、安全敏感度）
- 扫描结果正常写入 risks/ 文件（如发现风险）
- 第 4 个 CR 起恢复正常触发逻辑（依赖 insights.md pattern 匹配）

**教学触发**：首次兜底扫描时，附 1 句教学——"风险扫描帮你在编码前发现隐患。随着项目积累经验（insights.md），扫描会更精准。"

## 风险文件创建规则

1. 编号：扫描 `.devpace/risks/` 现有文件，取最大编号 +1（无文件从 001 开始）
2. 目录不存在时自动创建 `.devpace/risks/`
3. 格式遵循 `knowledge/_schema/auxiliary/risk-format.md`
4. 关联 CR 字段：如果是在 CR 开发中发现的，填写 CR 编号；否则填"无"
5. **教学触发**：首次创建风险文件时，检查 state.md taught 标记中是否含 `risk_file_created`，未含则附 1 句教学（见 teaching-catalog.md）

## 智能推荐模板（SSOT）

scan 和 monitor 报告末尾根据风险等级推荐下一步操作：

| 综合风险等级 | 推荐文案 |
|------------|---------|
| High | "建议：先处理高风险项再进入开发，或执行 `/pace-guard resolve` 标记已接受" |
| Medium | "建议：开发时关注这些风险点，可用 `/pace-guard monitor` 持续跟踪" |
| Low | "风险可控，可安全进入开发" |

## 降级模式（无 .devpace/）

无 `.devpace/` 时，scan/monitor 执行即时评估（不写文件，严重度矩阵仍适用）；trends/report/resolve 不可用（需要历史数据）。
