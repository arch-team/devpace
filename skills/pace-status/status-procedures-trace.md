# trace：反向追溯视图

> 由 SKILL.md 路由表加载。仅在 `trace <名称>` 子命令时读取。

从指定的 BR/PF/OBJ 出发，聚合所有下游实体状态（L2 深度视角，概念定义见 SKILL.md 首行）。

## 数据采集

1. 读取 `.devpace/project.md` 价值功能树，匹配名称或 ID
2. **Step 1: 定位**——通过 Glob+Grep 预筛选，定位匹配节点和相关 PF 文件列表
3. **Step 2: 采集**——从匹配节点出发，逐一读取下游 PF 及其关联 CR 状态
4. **Step 3: 聚合**——汇总 CR 完成率、验收标准通过率、MoS 达成率
5. **Step 4: 格式化**——按下方输出格式渲染
6. 读取已溢出的 PF 文件（`features/PF-xxx.md`，如存在）获取详细信息

## 匹配规则

| 输入 | 匹配方式 |
|------|---------|
| OBJ ID（如 `OBJ-1`） | 精确匹配树视图中的 OBJ 行 |
| BR ID（如 `BR-001`） | 精确匹配树视图中的 BR 行 |
| PF ID（如 `PF-001`） | 精确匹配树视图中的 PF 行 |
| 名称关键词（如 `用户认证`） | 模糊匹配 BR/PF/OBJ 名称，多匹配时列候选 |

## 输出格式

**从 OBJ 追溯**：
```
目标: [OBJ 名称] (OBJ-ID)
├── 需求: [BR 名称] (BR-ID)
│   ├── [PF 名称] (PF-ID) → N/M CR ✅
│   ├── [PF 名称] (PF-ID) → N/M CR 🔄
│   └── [PF 名称] (PF-ID) → N/M CR ⏳
└── 需求: [BR 名称] (BR-ID)
    └── [PF 名称] (PF-ID) → N/M CR ✅
MoS: X/Y 达成 ([达成的指标] ✅ | [未达成] ⏳)
```

**从 BR 追溯**：
```
需求: [BR 名称] (BR-ID) → [OBJ 名称]
├── [PF 名称] (PF-ID) → N/M CR ✅
├── [PF 名称] (PF-ID) → N/M CR 🔄
└── [PF 名称] (PF-ID) → N/M CR ⏳
MoS: X/Y 达成
```

**从 PF 追溯**：
```
功能: [PF 名称] (PF-ID)
所属: [BR 名称] → [OBJ 名称]
CR 状态: N/M 完成
验收标准: X/Y 通过
关联 CR:
  ├── [CR 标题] ✅ merged
  ├── [CR 标题] 🔄 developing
  └── [CR 标题] ⏳ created
```

## 规则

- **自然语言**：不暴露 ID 除非用户以 ID 查询
- **MoS 统计**：仅在追溯 OBJ 或 BR 时附加
- **已溢出 PF**：从 PF 文件追溯时展示完整验收标准通过率
- **无匹配**：提示"未找到匹配的业务需求/功能，请检查名称"

## 导航

输出末尾追加 1 行：`→ 全景价值链：/pace-status tree`
